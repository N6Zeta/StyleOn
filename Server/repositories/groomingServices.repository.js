//Model will be imported( firestore )

const getGroomingServiceData = async (res,req) => {
    /* 
        query to fetch data from firestore
        return the data to the service layer

    */
    const response = true;
    return response
}


module.exports = {
    getGroomingServiceData
}