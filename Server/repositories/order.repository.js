//Model will be imported( firestore )

const getorderData = async (res,req) => {
    /* 
        query to fetch data from firestore
        return the data to the service layer

    */
    console.log("calledd order repo")
    const response = true;
    return response
}


module.exports = {
    getorderData
}